#include <stdio.h>

int lower(int c);
int main()
{
    int c;
    while((c=getchar()) != EOF)
    {
        printf("%c",lower(c));
    }
   return 0;
}
int lower(int c)
{
    int low;
    low = c>='A' && c<='Z' ? c+'a'-'A' : c;
    return low;
}


output :

A
a
B
b
